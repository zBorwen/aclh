# 项目架构定义
modules:
  # 示例模块:
  # - name: "UserAuth"
  #   path: "src/modules/auth"
  #   responsibility: "处理用户登录、注册、鉴权"
  #   depends_on: ["Database", "EmailService"]
  #   owned_by: "team-alpha"
  #   public_api: ["login", "logout", "verifyToken"]
  #   submodules: ["OAuth", "JWT"]
  []

boundaries:
  rules:
    # 示例规则:
    # - "UI components MUST NOT directly access Database"
    []
  dependency_direction:
    # 示例依赖方向:
    # - "UI -> Domain -> Infrastructure"
    []
