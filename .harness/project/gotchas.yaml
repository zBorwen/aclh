# 框架/环境陷阱记录
entries:
  # 示例陷阱:
  # - id: "GOTCHA-001"
  #   category: "react" # react | typescript | css | build | deploy | test
  #   title: "useEffect 中忘记清理定时器"
  #   description: "在组件卸载时如果不清理定时器，会导致内存泄漏和不可预期的行为。"
  #   wrong_pattern: |
  #     useEffect(() => {
  #       setInterval(() => {
  #         setCount(c => c + 1);
  #       }, 1000);
  #     }, []);
  #   correct_pattern: |
  #     useEffect(() => {
  #       const id = setInterval(() => {
  #         setCount(c => c + 1);
  #       }, 1000);
  #       return () => clearInterval(id);
  #     }, []);
  #   applies_to: ["src/components/**/*.tsx"]
  []
