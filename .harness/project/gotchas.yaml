# 框架/环境陷阱记录。
# P2 retrieval metadata: category/tags/modules/applies_to。
entries:
  # - id: "GOTCHA-001"
  #   category: "react" # react | typescript | css | build | deploy | test
  #   tags: ["react", "effect", "timer"]
  #   modules: ["Dashboard"]
  #   title: "useEffect 中忘记清理定时器"
  #   description: "组件卸载时不清理定时器会造成泄漏和非预期更新。"
  #   wrong_pattern: "setInterval without cleanup"
  #   correct_pattern: "return () => clearInterval(id)"
  #   applies_to: ["src/components/**/*.tsx"]
  []
