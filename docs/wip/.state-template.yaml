task_id: "{{TASK_ID}}"
risk_level: "{{RISK_LEVEL}}" # L0 | L1 | L2 | L3
verification_strategy: "{{VERIFICATION_STRATEGY}}" # P2 compatibility: tdd | component | config | migration | docs
classification:
  file: "classification.yaml"
skill_plan:
  file: "skill-plan.yaml"
planning:
  contract: "spec-plan-tasks-v1"
identity:
  branch: "{{BRANCH}}"
  base_commit: "{{BASE_COMMIT}}"
  pr_number: null
context_scope:
  modules: []
  tags: []
  files: []
created_at: "{{CREATED_AT}}"
updated_at: "{{UPDATED_AT}}"
phase: "requirements" # requirements | design | task | implement | testing | delivery
status: "active"      # active | paused | blocked
review_history: []
self_review:
  packet: "self-review-packet.md"
  record: "self-review.json"
